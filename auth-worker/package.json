{
  "name": "auth-worker",
  "version": "1.0.0",
  "description": "Authentication worker for OTP-based authentication with KV store",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "coverage": "vitest run --coverage",
    "test:email": "node test-email.js",
    "test:deployed": "node run-tests.js",
    "test:all-envs": "node run-tests.js --all",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "deploy:test:preview": "./deploy-and-test.sh --env preview",
    "deploy:test:staging": "./deploy-and-test.sh --env staging",
    "deploy:test:production": "./deploy-and-test.sh --env production",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit",
    "tail": "wrangler tail",
    "tail:preview": "wrangler tail --env preview",
    "tail:staging": "wrangler tail --env staging",
    "tail:production": "wrangler tail --env production"
  },
  "keywords": [
    "cloudflare-workers",
    "authentication",
    "otp",
    "kv"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "eslint": "^8.57.0",
    "miniflare": "^3.20240129.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.4",
    "wrangler": "^4.32.0"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.873.0",
    "@aws-sdk/credential-providers": "^3.873.0",
    "hono": "^4.5.0",
    "jose": "^5.2.3"
  }
}
