{
  "name": "recipe-clipper-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for extracting recipes from URLs using GPT-OSS-20B",
  "main": "src/recipe-clipper.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:workers": "vitest run --config vitest.config.workers.js",
    "test:coverage": "vitest run --coverage",
    "coverage": "vitest run --coverage"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "recipe",
    "gpt",
    "ai",
    "extraction"
  ],
  "author": "Recipe App Team",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.65",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "happy-dom": "^18.0.1",
    "vitest": "^3.2.4",
    "wrangler": "^4.31.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
